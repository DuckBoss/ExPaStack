# A comma-separated list of mesh names to filter.
# default value: filter_list = []
filter_list = []

# Filters the mesh names so that the output json contains only the filtered names(include), or everything except the filtered names(exclude)
# This value can only be 'include' or 'exclude'.
# default value: filter_type = 'include'
filter_type = 'include'